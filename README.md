# Segmentation d'Images avec Hugging Face SegFormer

Ce notebook utilise l'API d'inférence Hugging Face pour effectuer
de la segmentation d'images avec le modèle SegFormer.

## Installation
pip install -r requirements.txt

## Configuration
Crée un fichier `.env` avec :
HF_API_TOKEN=ton_token_ici
